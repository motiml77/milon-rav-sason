# הוראות עבודה — מילון פנימיות התורה (רב ששון)

> **מסמך זה הוא reference ל-Claude.** כל פעם שהמשתמש מחליף את קובץ ה-Word (`ערכים/מושגים לאתר - עפ''י טללי חיים.docx`), הרץ את הצינור המתואר כאן בדיוק. כל הכללים שסיכמנו עם המשתמש כתובים כאן — אל תשנה אותם בלי בקשה מפורשת.

---

## 1. מבנה הפרויקט

```
rav sason/
├── milon.html              ← האתר כולו (CSS+JS inline). לפתוח עם שרת, לא מ-file://
├── terms.json              ← רשימת הערכים לסרגל (קל, נטען ראשון)
├── search-index.json       ← טקסט נקי לכל הגדרה, לחיפוש ולקטעי תצוגה (~5MB)
├── entries/t_*.json        ← תוכן מלא לכל ערך, נטען לפי דרישה (lazy)
├── sw.js                   ← Service Worker
├── assets/                 ← לוגו ותמונות
└── ערכים/
    ├── מושגים לאתר - עפ''י טללי חיים.docx   ← קלט המקור
    ├── convert_docx_hierarchical.py         ← Step 1: docx → data.js
    ├── convert_to_milon.py                  ← Step 2: data.js → כל פלטי האתר
    ├── build_search_index.py                ← מודול משותף: פורמט search-index/terms
    ├── rebuild_index_from_entries.py        ← בונה מחדש אינדקס מ-entries/ בלי ה-docx
    ├── eval/                                ← מדידת איכות החיפוש (golden set + scorer)
    ├── data.js                              ← פלט ביניים בתיקייה זו
    └── analyze_*.py / find_*.py / debug_*.py ← סקריפטי דיבוג עזר (לא חלק מהזרימה)

> **`data.json` הוסר.** ה-HTML לא טוען אותו יותר — הוא טוען `terms.json`
> ואז `entries/<id>.json` לפי דרישה. `ערכים/data.json` שנשאר בריפו הוא
> שארית ביניים ואינו בשימוש.
```

---

## 2. צינור ההמרה (Pipeline)

### ריצה אוטומטית (פקודה אחת)

```bash
cd 'C:\Users\Moti Levi\Desktop\AI\rav sason\ערכים'
python -c "
import os
import convert_docx_hierarchical as cdh
docx_path = os.path.abspath([f for f in os.listdir('.') if f.endswith('.docx') and not f.startswith('~$')][0])
data = cdh.convert_docx_to_hierarchical(docx_path)
cdh.save_to_js(data, 'data.js')
import convert_to_milon
"
```

זה עושה את כל השלבים:
1. **Step 1 — `convert_docx_hierarchical.py`**: קורא את ה-`.docx`, מפיק `ערכים/data.js`
2. **Step 2 — `convert_to_milon.py`**: קורא את `ערכים/data.js`, ומפיק:
   `search-index.json`, `terms.json`, `entries/*.json`, ומעדכן את מספר הגרסה
   ב-`sw.js` וב-`milon.html`. הפורמט נקבע ב-`build_search_index.py` (מודול משותף) —
   **אל תשכפל את הלוגיקה הזאת**, אחרת האינדקס ייפרד לשני פורמטים והחיפוש יישבר.

### הפעלת השרת לבדיקה

```bash
cd 'C:\Users\Moti Levi\Desktop\AI\rav sason'
python -m http.server 8765
```
ואז [http://localhost:8765/milon.html](http://localhost:8765/milon.html)

---

## 3. הנחיות לעריכת ה-Word

המשתמש מסמן דברים במסמך לפי האמנות הבאות. הסקריפט מזהה אותן אוטומטית.

### 3.1 היררכיה
- **Heading 1** (כותרת 1) = **מדור** (topic)
- **Heading 2** (כותרת 2) = **ערך** (term/entry)
- פסקה רגילה = הגדרה / פסקה של הגדרה

### 3.2 ציון מקור
- מתחיל ב-`[`
- מסתיים ב-`]`
- **מיושר לשמאל** ב-Word (זה מה שמבדיל אותו מהפניה inline)
- **בפסקה נפרדת** (לא בתוך פסקה של תוכן)
- הסקריפט מצרף אותו אוטומטית לפסקה הקודמת (גם דרך page break)

דוגמה: `[פרשת תזריע מצורע, שצד-שצה]` → יוצא לשדה `source` של ההגדרה האחרונה

### 3.3 הערה בתוך הטקסט
- בסוגריים עגולים `(...)`
- **גודל גופן ≤ 10pt** (קטן יותר מהטקסט הראשי)
- מוצג באתר ב-`<span class="text-small">` → font-size 0.78em, צבע עמום

דוגמה: `(חיה)`, `(ע"ח שער מב, פרק א מ"ב)`

### 3.4 הפניה לערך אחר
- בסוגריים מרובעים `[ראה ערך X]` (או `[עיין ערך X]`)
- בכתב קטן (גודל 9pt בדרך כלל)
- **אינה מיושרת לשמאל** (זה מה שמבדיל אותה ממקור)
- ה-X חייב להתאים לשם של ערך קיים במילון
- תומך ב-variants: `ראה ערך X`, `וראה ערך X`, `ראה לעיל ערך X`, `עיין ערך X`, `וכן ערך X`
- מסומנת בעיצוב ב-`<span class="text-small">` ו**גם** מוסיפה את הערך ל-`related` בתחתית

### 3.5 ציטוט מתוך מקור
שלוש דרכים מסומן ב-Word, כולן יזוהו:

**א. פסקה שלמה בגופן Aptos** (במקום הגופן הברירת מחדל)

**ב. פסקה שלמה בגודל 10.5pt או 11.5pt** (קטן יותר מהטקסט הראשי בקצת)

**ג. ציטוט תחום בגרשיים `"..."`** של לפחות **30 מילים** — נזהה אוטומטית גם בלי סימון גופן

הכלל לזיהוי a/b: יותר מ-50% מאורך הפסקה צריך להיות בגופן/גודל הציטוט.

### 3.6 הדגשות
- **Bold** (מודגש) → `<strong>`
- **Underline** (קו תחתון) → `<u>`
- טקסט עם קו תחתון משמש גם **כסימון להפניה** (אם זה שם של ערך קיים, ייכנס ל-`related` עם התאמה מדויקת)

---

## 4. עיצוב באתר (milon.html) — תקציר ה-CSS

| מרכיב | עיצוב |
|---|---|
| **שם הערך** | font-size 56px, font-weight 900 |
| **טקסט הגדרה** | Frank Ruhl Libre 19px, line-height 1.95, justify |
| **מקור** | font-size 13px, italic, ✦ + פס תחתון זהב |
| **`.text-small`** (סוגריים עגולים, הפניות) | 0.78em, צבע ink-soft |
| **`.quote`** (ציטוט) | Heebo Light 0.92em, רקע gradient עדין, גבול ימני זהב 3px |
| **drop-cap (אות ראשונה אדומה)** | רק ב-`.definition.first` — **לא** מופעל כשההגדרה מתחילה ב-`[` |
| **קו מפריד בין הגדרות** | `.definition.new-block` — **רק** אחרי הגדרה שיש לה מקור |
| **רווח בין פסקאות באותה הגדרה** | 10px |
| **רווח בין הגדרות שונות** | 28px + קו מקווקו |

---

## 5. הלוגיקה של `convert_to_milon.py` — פירוט

### שלב 1: בניית מבנה
- ממיר `sections` → `topics`, `name` → `title`+`subtitle`, `terms` → `entries`, `definition` → `definitions[{text, source}]`
- מפצל `[מקור]` בסוף הטקסט לשדה נפרד (`source`)
- פיצול בטוח: בודק שאין `<span>` פתוח לפני ה-`[` (כדי לא לחתוך הפניה inline)

### שלב 2: זיהוי ציטוטים inline
- regex `QUOTE_INLINE` תופס `: "..."` ארוך
- מטפל ב-:
  - קיצורים פנימיים (`הוי"ה`, `הא"ס`) — `"` באמצע מילה ≠ סוגר
  - HTML attributes (`class="text-small"`) — `"` בתוך תג ≠ סוגר
- **סף: ≥ 30 מילים** (משתנה `QUOTE_MIN_WORDS`)
- עוטף ב-`<span class="quote">`

### שלב 3: סריקת הפניות
- מאתר `[ראה ערך X]` בתוך הטקסט (גם אם עטוף ב-spans)
- **גם** טקסט עם קו תחתון `<u>X</u>` — אם תואם בדיוק שם ערך
- מטפל ב-:
  - שני ערכים בהפניה אחת (`[X; וראה ערך Y]`, `[X, וכן ערך Y]`)
  - שמות עם פסיק (לא מתבלבל)
  - חיתוך ביטויי-זנב (`בהערה שם`, `שם בפירוש`)
  - התאמת prefix (`עבודת הייחודים` → `עבודת הייחודים – מדרגת...`)
  - התעלמות מ-`שם` / `לעיל` / `לקמן`
- מוסיף את ה-IDs המתאימים ל-`entry.related`

### פרמטרים שאפשר לשנות (עם אישור המשתמש)

```python
QUOTE_MIN_WORDS = 30   # סף מינימלי למילים בציטוט inline
```

---

## 6. רינדור ב-milon.html — לוגיקה ב-JS

### הוספת מחלקות ל-`.definition`
```js
// 1. drop-cap (האות הראשונה האדומה) — רק להגדרה ראשונה שלא מתחילה ב-[
let cls = (i === 0 && !startsWithBracket) ? 'definition first' : 'definition';
// 2. קו מפריד — רק לפני הגדרה שההגדרה הקודמת לה הסתיימה במקור
if (i > 0 && entry.definitions[i-1].source) cls += ' new-block';
```

### הוספת בלוק `related`
- מופיע רק אם `entry.related` לא ריק
- כל ערך שלא נמצא במילון מסונן (`if(!r) return ''`)

### תצוגת מקור
- מופיע רק אם `d.source` לא ריק
- אחרת לא מציגים את ה-source-line בכלל

---

## 7. כללים שסוכמו עם המשתמש (חשוב!)

1. **לא למחוק את `[ראה ערך X]` מתחילת הגדרות** — להשאיר אותו (יוצג בקטן ועמום), רק לבטל את ה-drop-cap
2. **drop-cap** רק להגדרה ראשונה שאינה מתחילה בסוגריים
3. **קו מפריד בין הגדרות** רק כש-`entry.definitions[i-1].source` קיים
4. **ציטוטים inline** מינימום 30 מילים
5. **לוגו ושם המילון בסרגל** — בגודל קטן (50% מהמקור)
6. **גובה max של רשימת ערכים** = 50000px (כדי שיוצגו כל ה-590 הערכים)
7. **ה-folio (ספירת עמודים)** מוסר לחלוטין

---

## 8. מבנה הפלטים

### `search-index.json` — לחיפוש
```json
{
  "v": "2026-07-29-205241",
  "topics": ["ערכים כלליים"],
  "entries": [
    { "i": "t_1", "t": "נרנח\"י", "p": 0,
      "d": ["טקסט נקי מלא של ההגדרה הראשונה ✦ המקור", "הגדרה שנייה …"] }
  ]
}
```
`d` = טקסט נקי (בלי HTML), הגדרה אחת לכל פריט, כולל ציון המקור אחרי `✦`.
**כל הנרמול והטוקניזציה לחיפוש קורים ב-JS בלבד** — מקור אמת יחיד. בעבר הם
שוכפלו ל-Python (`termN`/`defsN`) וכל פער בין השניים יצר באגים שקטים.

### `entries/t_<n>.json` — תוכן מלא לערך
```json
{ "id": "t_1", "term": "נרנח\"י", "topicId": "s_1", "topicTitle": "ערכים כלליים",
  "definitions": [{ "text": "<HTML …>", "source": "פרשת תזריע מצורע, שצד-שצה" }],
  "related": ["t_524", "t_255"] }
```

### `terms.json` — לסרגל הצדדי
רק `{id, term}` לכל ערך, בתוך `topics`.

---

## 8א. מדידת איכות החיפוש

```bash
node "ערכים/eval/run-eval.mjs"                 # ציון נוכחי
node "ערכים/eval/run-eval.mjs" --verbose        # דירוג לכל שאילתה
node "ערכים/eval/run-eval.mjs" --save myname    # שמור מצב להשוואה
node "ערכים/eval/run-eval.mjs" --diff myname    # מה שופר / נסוג
```
מריץ את המנוע האמיתי מתוך `milon.html` (ב-Node, בלי דפדפן) מול
`ערכים/eval/golden.json`. **כל שינוי בדירוג החיפוש חייב לעבור כאן לפני push** —
אחרת אין דרך לדעת אם הוא שיפר או הרס.

## 9. בעיות נפוצות ותיקונים

| תופעה | סיבה | פתרון |
|---|---|---|
| שמות מדורים מופיעים כ-"מדור 2" | אין כותרת אמיתית ב-Heading 1 | לבקש מהמשתמש למלא ב-Word |
| ציטוט לא מזוהה | אין סימון גופן/גודל ב-Word, ופחות מ-30 מילים | הסבר למשתמש; אפשרות: הקטנת `QUOTE_MIN_WORDS` |
| `[ראה ערך X]` לא בולט | X לא מופיע במילון | לוודא שיש ערך כזה ב-Word |
| המקור לא נצמד | פסקת המקור לא מיושרת לשמאל | לתקן ב-Word |
| הערה בסוגריים עגולים בגודל רגיל | לא הוקטן ב-Word ל-≤10pt | לתקן ב-Word |

---

## 10. סטטיסטיקת התוצאה הנוכחית (להשוואה)

נכון לרגע כתיבת המסמך:
- 1 מדור
- 653 ערכים
- 4,578 הגדרות
- 2,941 (69%) עם מקור
- 246 ערכים עם הפניות (`related`)
- 406 הפניות סה"כ
- 71 ציטוטים שזוהו לפי גופן/גודל ב-Word
- 361 ציטוטים inline (`"..."` של ≥30 מילים)

אם המספרים יורדים משמעותית אחרי החלפת ה-Word — חקירה לפני שינוי קוד.

---

## 11. פריסה — GitHub + Vercel

**כתובת האתר הפעיל:** https://milon-rav-sason.vercel.app

**ריפו גיטהב:** https://github.com/motiml77/milon-rav-sason

Vercel מחובר לגיטהב ופורס אוטומטית כל push לענף `master`.

### תהליך עדכון (כל פעם שהוורד מתעדכן)

```bash
# שלב 1 — הרץ את הצינור
cd 'C:\Users\Moti Levi\Desktop\AI\rav sason\ערכים'
python -c "
import os, convert_docx_hierarchical as cdh
docx_path = os.path.abspath([f for f in os.listdir('.') if f.endswith('.docx') and not f.startswith('~$')][0])
data = cdh.convert_docx_to_hierarchical(docx_path)
cdh.save_to_js(data, 'data.js')
import convert_to_milon
"

# שלב 2 — העלה לגיטהב (Vercel יפרוס אוטומטית תוך ~30 שניות)
cd 'C:\Users\Moti Levi\Desktop\AI\rav sason'
git add search-index.json terms.json entries/ sw.js milon.html
git commit -m "עדכון תוכן מהוורד"
git push
```

> `sw.js` ו-`milon.html` נכללים כי הצינור מעדכן בהם את מספר הגרסה
> (בלעדיו הדפדפן יגיש למשתמשים חוזרים אינדקס ישן).
